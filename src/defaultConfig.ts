const config = {
  'create.dir': 'app',
  'template.default': '',
  'store.path': '.spore',
};

export const desc = {
  'create.dir': 'default dir which app will be created at',
  'template.default': 'default template in use',
  'store.path': 'config file save path which relative to "~/"',
};

export default config;
